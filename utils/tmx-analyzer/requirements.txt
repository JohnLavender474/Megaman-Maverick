# analyze.py uses only the Python standard library (argparse, json, xml.etree).
# This file exists to keep the same structure as utils/diagnostics so run.sh
# can be reused unchanged. Add packages here if the analyzer grows new deps.
